== README

Rails 4.2.0 with bootswatch, and Theme `cerulean` installed.

`rails g bootswatch:install cerulean`

`rails g scaffold product name description price:decimal`

`rails g bootswatch:themed Products`

